const config = {
	content: ['./src/**/*.{html,js,svelte,ts}'],

	theme: {
		extend: {
			fontFamily: {
				"blinker":["Blinker","sans-serif"],
			},
		}
	},

	plugins: []
};

module.exports = config;
